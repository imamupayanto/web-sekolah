<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>SD Nusa Bangsa — Portal Belajar</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: system-ui, sans-serif; background: #f5f5f5; color: #1a1a1a; }

    .navbar {
      background: #fff;
      border-bottom: 1px solid #e5e5e5;
      padding: 0 1.5rem;
      display: flex;
      align-items: center;
      justify-content: space-between;
      height: 56px;
      position: sticky; top: 0; z-index: 10;
    }
    .logo { font-size: 16px; font-weight: 600; }
    .logo span { color: #185FA5; }
    .nav-links { display: flex; gap: 1.5rem; }
    .nav-links a { font-size: 13px; color: #555; text-decoration: none; cursor: pointer; }
    .nav-links a:hover { color: #1a1a1a; }

    .hero {
      background: #185FA5;
      color: #fff;
      padding: 3rem 1.5rem;
      text-align: center;
    }
    .hero h1 { font-size: 26px; font-weight: 700; margin-bottom: 10px; }
    .hero p { font-size: 15px; opacity: 0.85; max-width: 480px; margin: 0 auto 1.5rem; }
    .btn-white { background: #fff; color: #185FA5; border: none; padding: 10px 24px; border-radius: 8px; font-size: 14px; font-weight: 600; cursor: pointer; margin-right: 8px; }
    .btn-white:hover { background: #e6f1fb; }
    .btn-outline-white { background: transparent; color: #fff; border: 1.5px solid rgba(255,255,255,0.6); padding: 10px 24px; border-radius: 8px; font-size: 14px; cursor: pointer; }

    .section { padding: 1.5rem; max-width: 800px; margin: 0 auto; }
    .section-title { font-size: 16px; font-weight: 600; margin-bottom: 1rem; }
    .cards { display: grid; grid-template-columns: repeat(auto-fit, minmax(160px, 1fr)); gap: 12px; margin-bottom: 1.5rem; }
    .card { background: #fff; border: 1px solid #e5e5e5; border-radius: 12px; padding: 1rem; cursor: default; }
    .card.clickable { cursor: pointer; }
    .card.clickable:hover { border-color: #185FA5; }
    .card-icon { font-size: 22px; margin-bottom: 8px; }
    .card h3 { font-size: 14px; font-weight: 600; margin-bottom: 4px; }
    .card p { font-size: 12px; color: #666; }
    .badge { display: inline-block; font-size: 11px; padding: 2px 8px; border-radius: 6px; margin-bottom: 8px; font-weight: 500; }
    .badge-blue { background: #E6F1FB; color: #185FA5; }
    .badge-green { background: #EAF3DE; color: #3B6D11; }
    .badge-amber { background: #FAEEDA; color: #854F0B; }

    /* QUIZ */
    #quiz-section { display: none; padding: 1.5rem; max-width: 600px; margin: 0 auto; }
    .back-btn { font-size: 13px; color: #555; background: none; border: none; cursor: pointer; margin-bottom: 1rem; padding: 0; }
    .back-btn:hover { color: #1a1a1a; }
    .quiz-box { background: #fff; border: 1px solid #e5e5e5; border-radius: 12px; padding: 1.25rem; }
    .quiz-header { display: flex; justify-content: space-between; font-size: 13px; color: #666; margin-bottom: 10px; }
    .progress-bar { width: 100%; height: 5px; background: #eee; border-radius: 4px; margin-bottom: 1.25rem; }
    .progress-fill { height: 5px; background: #185FA5; border-radius: 4px; transition: width 0.3s; }
    .question { font-size: 15px; font-weight: 600; line-height: 1.6; margin-bottom: 1rem; }
    .options { display: flex; flex-direction: column; gap: 8px; margin-bottom: 1rem; }
    .option { padding: 10px 14px; border: 1px solid #ddd; border-radius: 8px; font-size: 14px; text-align: left; background: #fff; cursor: pointer; }
    .option:hover:not(:disabled) { background: #f5f5f5; border-color: #aaa; }
    .option.correct { background: #EAF3DE; border-color: #639922; color: #2d5a0e; font-weight: 500; }
    .option.wrong { background: #FCEBEB; border-color: #E24B4A; color: #7a1f1f; }
    .option:disabled { cursor: default; }
    .feedback { font-size: 13px; padding: 10px 14px; border-radius: 8px; margin-bottom: 1rem; line-height: 1.5; display: none; }
    .feedback.show { display: block; }
    .feedback.correct { background: #EAF3DE; color: #2d5a0e; }
    .feedback.wrong { background: #FCEBEB; color: #7a1f1f; }
    .quiz-nav { display: flex; justify-content: flex-end; }
    .btn-primary { background: #185FA5; color: #fff; border: none; padding: 10px 20px; border-radius: 8px; font-size: 14px; cursor: pointer; font-weight: 500; display: none; }
    .btn-primary:hover { background: #0C447C; }

    .result-box { display: none; text-align: center; padding: 2rem 1rem; }
    .result-score { font-size: 48px; font-weight: 700; color: #185FA5; }
    .result-label { font-size: 14px; color: #666; margin-bottom: 6px; }
    .result-msg { font-size: 14px; color: #444; margin-bottom: 1.5rem; max-width: 360px; margin-left: auto; margin-right: auto; }
    .btn-restart { background: #185FA5; color: #fff; border: none; padding: 10px 20px; border-radius: 8px; font-size: 14px; cursor: pointer; margin-right: 8px; }
    .btn-back2 { background: transparent; color: #555; border: 1px solid #ccc; padding: 10px 20px; border-radius: 8px; font-size: 14px; cursor: pointer; }
  </style>
</head>
<body>

<nav class="navbar">
  <div class="logo">SD <span>Nusa Bangsa</span></div>
  <div class="nav-links">
    <a onclick="showHome()">Beranda</a>
    <a onclick="showQuiz()">Latihan Soal</a>
    <a>Materi</a>
  </div>
</nav>

<!-- HOME -->
<div id="home-section">
  <div class="hero">
    <h1>Selamat Datang di Portal Belajar</h1>
    <p>Akses materi pelajaran dan latihan soal interaktif untuk siswa kelas 1–6</p>
    <button class="btn-white" onclick="showQuiz()">Mulai Latihan</button>
    <button class="btn-outline-white">Lihat Materi</button>
  </div>

  <div class="section">
    <div class="section-title">Mata Pelajaran</div>
    <div class="cards">
      <div class="card">
        <div class="card-icon">📐</div>
        <div class="badge badge-blue">Kelas 4–6</div>
        <h3>Matematika</h3>
        <p>Operasi hitung, pecahan, dan geometri</p>
      </div>
      <div class="card">
        <div class="card-icon">🔬</div>
        <div class="badge badge-green">Kelas 4–6</div>
        <h3>IPA</h3>
        <p>Makhluk hidup, cuaca, dan energi</p>
      </div>
      <div class="card">
        <div class="card-icon">🌏</div>
        <div class="badge badge-amber">Kelas 4–6</div>
        <h3>IPS</h3>
        <p>Sejarah, geografi, dan kebudayaan</p>
      </div>
      <div class="card">
        <div class="card-icon">📖</div>
        <div class="badge badge-blue">Semua kelas</div>
        <h3>Bahasa Indonesia</h3>
        <p>Membaca, menulis, dan tata bahasa</p>
      </div>
    </div>

    <div class="section-title">Latihan Soal Tersedia</div>
    <div class="cards">
      <div class="card clickable" onclick="showQuiz()">
        <h3>IPA Kelas 5</h3>
        <p>Sistem tata surya</p>
        <p style="margin-top:8px;font-size:11px;color:#185FA5;font-weight:500">5 soal pilihan ganda →</p>
      </div>
      <div class="card">
        <h3>Matematika Kelas 4</h3>
        <p>Pecahan sederhana</p>
        <p style="margin-top:8px;font-size:11px;color:#aaa">Segera hadir</p>
      </div>
      <div class="card">
        <h3>B. Indonesia Kelas 5</h3>
        <p>Antonim &amp; sinonim</p>
        <p style="margin-top:8px;font-size:11px;color:#aaa">Segera hadir</p>
      </div>
    </div>
  </div>
</div>

<!-- QUIZ -->
<div id="quiz-section">
  <button class="back-btn" onclick="showHome()">← Kembali ke beranda</button>
  <div style="font-size:16px;font-weight:600;margin-bottom:1rem">Latihan Soal — IPA Kelas 5: Tata Surya</div>

  <div class="quiz-box" id="quiz-main">
    <div class="quiz-header">
      <span id="q-label">Soal 1 dari 5</span>
      <span id="score-label">Nilai: 0</span>
    </div>
    <div class="progress-bar"><div class="progress-fill" id="progress" style="width:20%"></div></div>
    <div class="question" id="question-text"></div>
    <div class="options" id="options-list"></div>
    <div class="feedback" id="feedback"></div>
    <div class="quiz-nav">
      <button class="btn-primary" id="next-btn" onclick="nextQuestion()">Soal berikutnya →</button>
    </div>
  </div>

  <div class="result-box" id="result-box">
    <div class="result-label">Skor kamu</div>
    <div class="result-score" id="result-score"></div>
    <p class="result-msg" id="result-msg"></p>
    <button class="btn-restart" onclick="restartQuiz()">Ulangi latihan</button>
    <button class="btn-back2" onclick="showHome()">Kembali ke beranda</button>
  </div>
</div>

<script>
const questions = [
  {q:"Planet terbesar dalam tata surya adalah...",opts:["Bumi","Saturnus","Jupiter","Uranus"],ans:2,exp:"Jupiter adalah planet terbesar, volumenya lebih dari 1.300 kali Bumi."},
  {q:"Berapa jumlah planet dalam tata surya kita?",opts:["7","8","9","10"],ans:1,exp:"Ada 8 planet: Merkurius, Venus, Bumi, Mars, Jupiter, Saturnus, Uranus, Neptunus."},
  {q:"Planet yang paling dekat dengan Matahari adalah...",opts:["Venus","Mars","Bumi","Merkurius"],ans:3,exp:"Merkurius adalah planet terdekat dari Matahari."},
  {q:"Benda langit yang mengelilingi planet disebut...",opts:["Bintang","Satelit","Komet","Meteor"],ans:1,exp:"Satelit adalah benda langit yang mengorbit planet, contohnya Bulan mengorbit Bumi."},
  {q:"Proses Bumi mengelilingi Matahari disebut...",opts:["Rotasi","Revolusi","Gravitasi","Orbit"],ans:1,exp:"Revolusi adalah gerakan Bumi mengelilingi Matahari, membutuhkan sekitar 365 hari."}
];

let cur=0, score=0, answered=false;

function loadQuestion(){
  const q=questions[cur];
  document.getElementById('q-label').textContent='Soal '+(cur+1)+' dari '+questions.length;
  document.getElementById('score-label').textContent='Nilai: '+score;
  document.getElementById('progress').style.width=(((cur+1)/questions.length)*100)+'%';
  document.getElementById('question-text').textContent=q.q;
  const ol=document.getElementById('options-list');
  ol.innerHTML='';
  q.opts.forEach(function(o,i){
    const btn=document.createElement('button');
    btn.className='option';
    btn.textContent=o;
    btn.onclick=function(){answer(i);};
    ol.appendChild(btn);
  });
  const fb=document.getElementById('feedback');
  fb.className='feedback'; fb.textContent='';
  document.getElementById('next-btn').style.display='none';
  answered=false;
}

function answer(i){
  if(answered)return;
  answered=true;
  const q=questions[cur];
  const btns=document.querySelectorAll('.option');
  btns.forEach(function(b){b.disabled=true;});
  const fb=document.getElementById('feedback');
  if(i===q.ans){
    btns[i].classList.add('correct');
    score++;
    fb.className='feedback show correct';
    fb.textContent='✓ Benar! '+q.exp;
  } else {
    btns[i].classList.add('wrong');
    btns[q.ans].classList.add('correct');
    fb.className='feedback show wrong';
    fb.textContent='✗ Kurang tepat. '+q.exp;
  }
  document.getElementById('score-label').textContent='Nilai: '+score;
  const nb=document.getElementById('next-btn');
  nb.style.display='block';
  nb.textContent=cur<questions.length-1?'Soal berikutnya →':'Lihat hasil';
}

function nextQuestion(){
  cur++;
  if(cur>=questions.length){showResult();return;}
  loadQuestion();
}

function showResult(){
  document.getElementById('quiz-main').style.display='none';
  document.getElementById('result-box').style.display='block';
  const pct=Math.round((score/questions.length)*100);
  document.getElementById('result-score').textContent=score+'/'+questions.length+' ('+pct+'%)';
  let msg='';
  if(pct===100)msg='Sempurna! Kamu menguasai materi ini dengan sangat baik.';
  else if(pct>=60)msg='Bagus! Terus berlatih untuk hasil yang lebih baik.';
  else msg='Jangan menyerah! Coba pelajari lagi materinya ya.';
  document.getElementById('result-msg').textContent=msg;
}

function restartQuiz(){
  cur=0;score=0;answered=false;
  document.getElementById('quiz-main').style.display='block';
  document.getElementById('result-box').style.display='none';
  loadQuestion();
}

function showHome(){
  document.getElementById('home-section').style.display='block';
  document.getElementById('quiz-section').style.display='none';
}

function showQuiz(){
  document.getElementById('home-section').style.display='none';
  document.getElementById('quiz-section').style.display='block';
  restartQuiz();
}

loadQuestion();
</script>
</body>
</html>
